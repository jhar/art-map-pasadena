var assert = chai.assert();

describe('What are you testing?', function() {
	it('What is expected?', function() {
		// Do things, generate actual & expected values

		assert.equal(actual, expected, 'Message');
	});
});